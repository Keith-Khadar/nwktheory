import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nkwtheory.app',
  appName: 'nwktheory',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
